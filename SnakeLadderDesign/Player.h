#pragma once
#include <iostream>
#include <string>

using namespace std;

class Player{
    public:
    string name;
    int currPos;
    public:
        Player(string name, int currPos):name(name),currPos(currPos){}

};


